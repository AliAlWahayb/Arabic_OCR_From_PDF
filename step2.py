from combine import combine_text_files
from tashkeel import harakat
from formatting_custom import formatting_custom

directory = "" # folder path



formatting_custom(directory)

# harakat(directory)

# combine_text_files(directory, f"{directory}/combined.txt")

print("done")